import { PlayerStatePipe } from './player-state.pipe';

describe('PlayerStatePipe', () => {
  it('create an instance', () => {
    const pipe = new PlayerStatePipe();
    expect(pipe).toBeTruthy();
  });
});
